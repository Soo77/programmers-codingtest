Java 의 해시코드란?
객체를 식별할 수 있는 유니크한 값을 말한다. 메모리에 생성된 객체의 주소를 정수로 변환한 형태를
얘기하는데, 이 정수는 중복되지 않는 고유의 값이다.
Object Class에 hashCode()를 가지고 있어서 객체를 생성하여 확인해보면 Hash Code 값을
확인할 수 있다.

동일성 비교는 == 비교. 객체 인스턴스의 주소 값을 비교한다.
(primitive data type의 경우 ==을 통해 값 비교가 가능하다.)

동등성 비교는 equals() 메소드를 사용해서 객체 내부의 값을 비교한다.

